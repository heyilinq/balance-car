/**
 * ============================================================
 * B570 平衡小车 - 编码器速度测量模块
 * ============================================================
 * TIM3/TIM4 硬件编码器模式：自动计数 AB 相脉冲（4倍频）
 * 13线编码器 × 30减速比 × 67mm轮径
 * Read_Encoder(): 读 CNT → 强转(short)处理正反转 → 清零
 * 速度换算在 control.c 的 Get_Velocity_Form_Encoder() 中完成
 * ============================================================
 */

#include "encoder.h"

/**************************************************************************
函数功能：单位时间读取编码器计数
入口参数：定时器
返回  值：速度值
**************************************************************************/
int Read_Encoder(u8 TIMX)
{
    int Encoder_TIM;    
    switch(TIMX)
	 {
	   case 3:  Encoder_TIM= (short)TIM3 -> CNT;  TIM3 -> CNT=0; break;
	   case 4:  Encoder_TIM= (short)TIM4 -> CNT;  TIM4 -> CNT=0; break;	
	   default:  Encoder_TIM=0;
	 }
		return Encoder_TIM;
}
///**************************************************************************
//函数功能：TIM3中断服务函数
//入口参数：无
//返回  值：无
//**************************************************************************/
//void TIM4_IRQHandler(void)//中断处理函数为空，清除中断标志位后结束中断
//{ 		    		  			    
// if(TIM_GetFlagStatus(TIM4,TIM_FLAG_Update)==SET)//溢出中断
// {
//	 
// } 
// TIM_ClearITPendingBit(TIM4,TIM_IT_Update); //清除中断标志位 	
//}
///**************************************************************************
//函数功能：TIM2中断服务函数
//入口参数：无
//返回  值：无
//**************************************************************************/
//void TIM2_IRQHandler(void)//中断处理函数为空，清除中断标志位后结束中断
//{ 		    		  			    
// if(TIM_GetFlagStatus(TIM2,TIM_FLAG_Update)==SET)//溢出中断
// {
//	 
// } 
// TIM_ClearITPendingBit(TIM2,TIM_IT_Update); //清除中断标志位 	  
//}
